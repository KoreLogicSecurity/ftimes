########################################################################
#
# $Id: Makefile.in,v 1.5 2005/05/24 20:31:31 mavrik Exp $
#
########################################################################
#
# Copyright 2003-2005 The FTimes Project, All Rights Reserved.
#
########################################################################
#
# Purpose: Makefile for the project's dig directory.
#
########################################################################

#.SILENT:

prefix			= @prefix@
exec_prefix		= @exec_prefix@
bindir			= @bindir@
srcdir			= @srcdir@
VPATH			= ${srcdir}

EXTENSION		= @extension@

INSTALL			= @INSTALL@

SCRIPT_FILES		=\
			ftimes-dig2ctx.pl\
			hipdig.pl

all:

test:

install: ${SCRIPT_FILES}
	@umask 22; if [ ! -d ${bindir} ]; then mkdir -p ${bindir}; fi
	@for i in ${SCRIPT_FILES}; do\
		if [ -f ${srcdir}/$${i} ]; then file=${srcdir}/$${i}; else file=$${i}; fi;\
		${INSTALL} -m 755 $${file} ${bindir};\
	done

clean:

clean-all: clean
	rm -f Makefile

